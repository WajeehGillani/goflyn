# Submission note

Markdown + Git make team knowledge readable, attributable, and reviewable.
Raw notes stay immutable; each compiled claim cites its source/contributor.
The LLM makes semantic proposals; Python owns provenance, conflicts, paths, locking, Git, and lint.
A conversational CLI exposes the same core ingest/query/lint functions used by tests and the demo loader.
Query reads the compiled wiki with bounded links and citations, not vector/chunk RAG.
Relevant unsafe conflict answers get one corrective retry, then a deterministic evidence fallback.
Explicit entity assertions create source-backed empty pages without inventing attributes.
Human-only resolution previews evidence and requires yes, preserves history, and commits reviewer/reason/time.
All operations share one conflict model/parser; query/lint distinguish active and historical conflicts.
A repository lock protects cooperating writers on one filesystem, not distributed nodes.
Nine fictional John/Sarah notes demonstrate real ingest commits and two intended contradictions.
An extraction mistake remains visible in historical memory; structured output is not semantic validation.
UI, MCP, authentication, deployment, and automatic truth resolution remain out of scope.
With a week: PR review, richer schema/retrieval, MCP, and evaluations/observability.
