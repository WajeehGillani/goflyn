# Optional default contributor; --user takes precedence.
GOFLIGHT_USER=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# To select another checkout, export GOFLIGHT_ROOT=/absolute/path/to/checkout
# in your shell before launch. Its .env will then be loaded.
